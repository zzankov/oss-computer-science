    #include <stdio.h> 
    int main(void) { 
        int i, t[4];
        for(i = 3; i; i--) {
                t[i] = i - 1;
                t[t[i]] = t[i];
        }
        printf("%d\n", t[0]);
        return 0; 
    }
